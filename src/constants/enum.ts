export enum StoreKey {
  Theme = 'theme',
}
export enum AccountType {
  Username, //用户名密码
  Email, //邮箱密码
  Wechat, //微信
  Github, //Github
}
export enum Sex {
  female, //女性
  male, //男性
}
export enum ResultCode {
  Success,
  failed,
}
