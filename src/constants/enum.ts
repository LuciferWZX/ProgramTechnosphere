export enum StoreKey {
  Token = 'pt-token',
  Theme = 'theme',
}
export enum AccountType {
  Username, //用户名密码
  Email, //邮箱密码
  Wechat, //微信
  Github, //Github
}
export enum Sex {
  Female, //女性
  Male, //男性
  TwoDimensional,
}
export enum ResultCode {
  Success,
  failed,
}
